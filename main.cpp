#include <iostream>
#include "Graph.h"
#define a 5

int main()
{
    /*
    // Tests ctor from file
    // automaton0.txt accepte tous les mots qui finissent pas C (alphabet = {A,B,C})
    Graph newGraph("automaton0.txt");
    newGraph.uglyPrint();


    // Tests words entries
    std::string word;
    std::cout << "enter your word: ";
    std::cin >> word;
    (newGraph.wordEntry(word)) ? std::cout<<"accepted" : std::cout<<"refused";
    */

    return 0;
}
